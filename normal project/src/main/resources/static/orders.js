document.addEventListener('DOMContentLoaded',async()=>{if(!StoreApi.token()){location.href='log in.html';return;}try{const orders=await StoreApi.request('/api/orders/my');const box=document.getElementById('orders-container');if(!orders.length){document.getElementById('no-orders-message').style.display='block';return;}orders.forEach(o=>{const d=document.createElement('div');d.className='card p-3 mb-3';d.innerHTML=`<h4>Order #${o.id}</h4><p>${new Date(o.createdAt).toLocaleString()}</p><p>${o.address}, ${o.city}, ${o.country} ${o.postalCode}</p><div>${o.items.map(i=>`<div class="d-flex align-items-center gap-3 border-top py-2"><img src="${i.imageUrl}" style="height:50px"><span>${i.productName} × ${i.quantity}</span><b class="ms-auto">$${(i.price*i.quantity).toFixed(2)}</b></div>`).join('')}</div><h5 class="text-end">Total: $${o.total.toFixed(2)}</h5>`;box.appendChild(d);});}catch(e){alert(e.message);}});
